{
  "//": "Type-CHECK only (noEmit). The project stays plain JS + Vite. Only files that opt in with a top-of-file `// @ts-check` are checked; everything else is ignored. Run `npm run typecheck`, or get inline errors in any editor with the TS language service.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "skipLibCheck": true,
    "types": []
  },
  "include": ["src", "test"],
  "exclude": ["node_modules", "dist"]
}
